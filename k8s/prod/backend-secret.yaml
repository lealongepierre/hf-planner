apiVersion: v1
kind: Secret
metadata:
  name: backend-secret
  namespace: hf-planner-prod
type: Opaque
stringData:
  # IMPORTANT: Replace these placeholder values before deploying to production!
  JWT_SECRET_KEY: "CHANGE_ME_IN_PRODUCTION"
  DATABASE_URL: "CHANGE_ME_CLOUD_SQL_CONNECTION_STRING"
  # Format: postgresql://USER:PASSWORD@CLOUD_SQL_IP:5432/DATABASE_NAME
