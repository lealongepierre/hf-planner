apiVersion: v1
kind: Service
metadata:
  name: frontend-service
  namespace: hf-planner-dev
spec:
  selector:
    app: frontend
  ports:
  - port: 80
    targetPort: 80
    protocol: TCP
  type: LoadBalancer  # For minikube, use: minikube tunnel
