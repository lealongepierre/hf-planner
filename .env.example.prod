# PostgreSQL
POSTGRES_USER=hfplanner
POSTGRES_PASSWORD=<SECURE_PASSWORD>
POSTGRES_DB=hfplanner

# Backend
DATABASE_URL=postgresql://hfplanner:<SECURE_PASSWORD>@postgres:5432/hfplanner
JWT_SECRET_KEY=<generate with: openssl rand -hex 32>
JWT_ALGORITHM=HS256
JWT_ACCESS_TOKEN_EXPIRE_MINUTES=30

# Domain for HTTPS (Caddy auto-provisions Let's Encrypt certificates)
# Leave empty or "localhost" for HTTP-only / local testing
DOMAIN=
